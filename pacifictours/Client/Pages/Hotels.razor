@page "/hotel"
@using pacifictours.Shared
@inject HttpClient Http
<h3>Hotels</h3>
@if (hotels.Count <= 0)
{
	<span>Loading Hotels…</span>
}
else
{
	<table>
		<thead>
			<tr>
				<th>ID</th>
				<th>Code</th>
				
				<th>Full Name</th>
				<th>Address</th>
				<th>Joining Date </th>
			</tr>
		</thead>
		<tbody>
			@foreach (var hotel in hotels)
			{
				<tr>
					<td width="5%">@hotel.Id</td>
					<td width="5%">@hotel.name</td>
					<td width="10%">@hotel.singleprice</td>
					<td width="10%">@hotel.doubleprice</td>
					<td width="5%">@hotel.familyprice</td>
					<td width="5%">@hotel.rooms</td>
				</tr>
			}
		</tbody>
	</table>
}

@code {
	List<Hotel> hotels = new List<Hotel>();
 protected override async Task OnInitializedAsync()
	{
		var result = await Http.GetFromJsonAsync<List<Hotel>>("/api/hotel");
		if (result != null)
		{
			hotels = result;
		}
	}
}
